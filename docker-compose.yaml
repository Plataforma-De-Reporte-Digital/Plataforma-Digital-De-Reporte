services:

db:
  image: postgres:15
  environment:
    POSTGRES_USER: postgres_ipt_issue
    POSTGRES_PASSWORD: postgres_ipt_password
    POSTGRES_DB: risk_management_db
  ports:
    - "5432:5432"
  volumes:
    - db_data:/var/lib/postgresql/data

volumes:
  db_data: